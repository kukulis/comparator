package gt.comparedata.model;

/**
 * You may use this as DNA elements, but then must remake DataComparator to use parametrization, or leave strings as it is now. 
 *
 */
public enum DNAElements {
	A, 
	G, 
	C, 
	T;
}
